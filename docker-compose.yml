version: "3.9"

services:
  frontend:
    build: ./frontend
    ports:
      - "5173:4173"

  auth-service:
    build: ./AuthService
    ports:
      - "5001:8080"

  main-service:
    build: ./MainService
    ports:
      - "5002:8080"
    depends_on:
      - auth-service